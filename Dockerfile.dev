FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci
# Source is mounted over /app at runtime; node_modules preserved via volume
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
